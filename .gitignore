# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
*.egg-info/
.DS_Store

# secrets - never commit
.env
.env.*
!.env.example

# runtime outputs (governed transcripts / audit from demo runs)
out/

# audio build artifacts (the .wav segments + manifest.json are committed)
audio/*.aiff
audio/_concat.txt
